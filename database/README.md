## Database Structure

The database folder contains the SQL files required to create and initialize the Food Rescue System database.

database/
├── schema.sql       # Creates all database tables
├── seed_data.sql    # Inserts initial/sample data
└── README.md        # Database documentation


```bash
cd backend
python seed.py
```
